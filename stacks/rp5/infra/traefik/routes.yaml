http:
  routers:
    komodo:
      rule: "Host(`doesntwork.beansrn.com`)"
      entryPoints:
        - web
        - websecure
      service: komodo

  services:
    komodo:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:9120"
